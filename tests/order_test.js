'use strict';

describe('posApp.order module', function() {

  beforeEach(module('posApp.order'));

  describe('order controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var orderCtrl = $controller('OrderController');
      expect(orderCtrl).toBeDefined();
    }));

  });
});
