'use strict';

describe('posApp.home module', function() {

  beforeEach(module('posApp.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('HomeController');
      expect(homeCtrl).toBeDefined();
    }));

  });
});
